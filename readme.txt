The SUBDOMAIN SEARCHER is simple to use.
Go to the command line and type:

python3 sds.py

You can change the file that contains the subdomains name, just edit the source line with your file path and it is done.